{
  "root": "libs/documentations",
  "sourceRoot": "libs/documentations",
  "projectType": "library",
  "targets": {
    "build-external-docs": {
      "executor": "@nrwl/workspace:run-commands",
      "options": {
        "commands": ["STORYBOOK_BASE_HREF=soba/ npx nx build-storybook soba", "npx nx build sandbox"],
        "parallel": true
      },
      "dependsOn": [
        {
          "target": "build",
          "projects": "self"
        }
      ]
    }
  },
  "tags": ["documentations"]
}
