import { PopmotionDirective } from './popmotion.directive';

describe('PopmotionDirective', () => {
  it('should create an instance', () => {
    const directive = new PopmotionDirective();
    expect(directive).toBeTruthy();
  });
});
