import { AudioListenerDirective } from './audio-listener.directive';

describe('AudioListenerDirective', () => {
  it('should create an instance', () => {
    const directive = new AudioListenerDirective();
    expect(directive).toBeTruthy();
  });
});
