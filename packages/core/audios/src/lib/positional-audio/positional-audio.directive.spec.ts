import { PositionalAudioDirective } from './positional-audio.directive';

describe('PositionalAudioDirective', () => {
  it('should create an instance', () => {
    const directive = new PositionalAudioDirective();
    expect(directive).toBeTruthy();
  });
});
