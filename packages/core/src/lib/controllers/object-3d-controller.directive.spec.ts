import { Object3dControllerDirective } from './object-3d-controller.directive';

describe('Object3dControllerDirective', () => {
  it('should create an instance', () => {
    const directive = new Object3dControllerDirective();
    expect(directive).toBeTruthy();
  });
});
