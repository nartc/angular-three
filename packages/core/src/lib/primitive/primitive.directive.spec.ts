import { PrimitiveDirective } from './primitive.directive';

describe('PrimitiveDirective', () => {
  it('should create an instance', () => {
    const directive = new PrimitiveDirective();
    expect(directive).toBeTruthy();
  });
});
