import { LineSegmentsDirective } from './line-segments.directive';

describe('LineSegmentsDirective', () => {
  it('should create an instance', () => {
    const directive = new LineSegmentsDirective();
    expect(directive).toBeTruthy();
  });
});
