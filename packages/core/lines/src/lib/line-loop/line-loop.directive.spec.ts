import { LineLoopDirective } from './line-loop.directive';

describe('LineLoopDirective', () => {
  it('should create an instance', () => {
    const directive = new LineLoopDirective();
    expect(directive).toBeTruthy();
  });
});
