import { RectAreaLightDirective } from './rect-area-light.directive';

describe('RectAreaLightDirective', () => {
  it('should create an instance', () => {
    const directive = new RectAreaLightDirective();
    expect(directive).toBeTruthy();
  });
});
