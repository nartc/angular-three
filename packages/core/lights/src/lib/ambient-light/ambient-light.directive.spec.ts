import { AmbientLightDirective } from './ambient-light.directive';

describe('AmbientLightDirective', () => {
  it('should create an instance', () => {
    const directive = new AmbientLightDirective();
    expect(directive).toBeTruthy();
  });
});
