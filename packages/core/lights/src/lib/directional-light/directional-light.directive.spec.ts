import { DirectionalLightDirective } from './directional-light.directive';

describe('DirectionalLightDirective', () => {
  it('should create an instance', () => {
    const directive = new DirectionalLightDirective();
    expect(directive).toBeTruthy();
  });
});
