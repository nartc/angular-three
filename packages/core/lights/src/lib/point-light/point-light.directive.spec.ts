import { PointLightDirective } from './point-light.directive';

describe('PointLightDirective', () => {
  it('should create an instance', () => {
    const directive = new PointLightDirective();
    expect(directive).toBeTruthy();
  });
});
