import { SpriteDirective } from './sprite.directive';

describe('SpriteDirective', () => {
  it('should create an instance', () => {
    const directive = new SpriteDirective();
    expect(directive).toBeTruthy();
  });
});
