import { SpriteMaterialDirective } from './sprite-material.directive';

describe('SpriteMaterialDirective', () => {
  it('should create an instance', () => {
    const directive = new SpriteMaterialDirective();
    expect(directive).toBeTruthy();
  });
});
