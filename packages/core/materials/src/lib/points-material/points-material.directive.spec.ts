import { PointsMaterialDirective } from './points-material.directive';

describe('PointsMaterialDirective', () => {
  it('should create an instance', () => {
    const directive = new PointsMaterialDirective();
    expect(directive).toBeTruthy();
  });
});
