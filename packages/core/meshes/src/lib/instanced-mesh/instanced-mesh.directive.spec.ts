import { InstancedMeshDirective } from './instanced-mesh.directive';

describe('InstancedMeshDirective', () => {
  it('should create an instance', () => {
    const directive = new InstancedMeshDirective();
    expect(directive).toBeTruthy();
  });
});
