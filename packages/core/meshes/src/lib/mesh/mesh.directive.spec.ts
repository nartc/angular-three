import { MeshDirective } from './mesh.directive';

describe('MeshDirective', () => {
  it('should create an instance', () => {
    const directive = new MeshDirective();
    expect(directive).toBeTruthy();
  });
});
