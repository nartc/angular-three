import { BoneDirective } from './bone.directive';

describe('BoneDirective', () => {
  it('should create an instance', () => {
    const directive = new BoneDirective();
    expect(directive).toBeTruthy();
  });
});
