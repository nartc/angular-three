import { SkinnedMeshDirective } from './skinned-mesh.directive';

describe('SkinnedMeshDirective', () => {
  it('should create an instance', () => {
    const directive = new SkinnedMeshDirective();
    expect(directive).toBeTruthy();
  });
});
