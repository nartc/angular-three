import { CylinderBufferGeometryDirective } from './cylinder-buffer-geometry.directive';

describe('CylinderBufferGeometryDirective', () => {
  it('should create an instance', () => {
    const directive = new CylinderBufferGeometryDirective();
    expect(directive).toBeTruthy();
  });
});
