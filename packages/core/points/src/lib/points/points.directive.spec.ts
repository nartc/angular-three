import { PointsDirective } from './points.directive';

describe('PointsDirective', () => {
  it('should create an instance', () => {
    const directive = new PointsDirective();
    expect(directive).toBeTruthy();
  });
});
