import { BufferAttributeDirective } from './buffer-attribute.directive';

describe('BufferAttributeDirective', () => {
  it('should create an instance', () => {
    const directive = new BufferAttributeDirective();
    expect(directive).toBeTruthy();
  });
});
