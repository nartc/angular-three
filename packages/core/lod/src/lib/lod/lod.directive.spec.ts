import { LodDirective } from './lod.directive';

describe('LodDirective', () => {
  it('should create an instance', () => {
    const directive = new LodDirective();
    expect(directive).toBeTruthy();
  });
});
