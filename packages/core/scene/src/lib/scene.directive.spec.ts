import { SceneDirective } from './scene.directive';

describe('SceneDirective', () => {
  it('should create an instance', () => {
    const directive = new SceneDirective();
    expect(directive).toBeTruthy();
  });
});
