import { ShaderPassDirective } from './shader-pass.directive';

describe('ShaderPassDirective', () => {
  it('should create an instance', () => {
    const directive = new ShaderPassDirective();
    expect(directive).toBeTruthy();
  });
});
