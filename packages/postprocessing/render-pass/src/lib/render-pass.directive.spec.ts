import { RenderPassDirective } from './render-pass.directive';

describe('RenderPassDirective', () => {
  it('should create an instance', () => {
    const directive = new RenderPassDirective();
    expect(directive).toBeTruthy();
  });
});
