import { SsaoPassDirective } from './ssao-pass.directive';

describe('SsaoPassDirective', () => {
  it('should create an instance', () => {
    const directive = new SsaoPassDirective();
    expect(directive).toBeTruthy();
  });
});
