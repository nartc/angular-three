import { UnrealBloomPassDirective } from './unreal-bloom-pass.directive';

describe('UnrealBloomPassDirective', () => {
  it('should create an instance', () => {
    const directive = new UnrealBloomPassDirective();
    expect(directive).toBeTruthy();
  });
});
